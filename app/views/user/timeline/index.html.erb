<h2>Seja Bem Vindo!</h2>

<div class="row">
    <div class="offset-2 col-lg-8">
        <% @posts.each do |post|%>
            <%= render "user/post_card" , post: post%>
        <% end%>
    </div>
</div>